/*
  App.tsx — Entry point for the corporate single-page website.
  Renders the Home component directly. No routing needed — this is a single page.
*/
import Home from "@/pages/Home";

export default function App() {
  return <Home />;
}
